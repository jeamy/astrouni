#pragma once

#include <wx/wx.h>
#include <vector>
#include "astro/orben_data.h"

class PlanetOrAspectSymbolPanel;

class OrbenDialog : public wxDialog {
public:
    OrbenDialog(wxWindow* parent, OrbenData& orben_data);

private:
    void OnOk(wxCommandEvent& event);
    void OnDefault(wxCommandEvent& event);
    void OnZap(wxCommandEvent& event);
    void OnCancel(wxCommandEvent& event);
    
    void LoadOrbenValues();      // Lädt Werte in die TextCtrls
    void SaveOrbenValues();      // Speichert Werte aus den TextCtrls
    void LoadDefaultValues();    // Lädt Default-Werte
    
    OrbenData& orben_data_;
    std::vector<wxTextCtrl*> text_ctrls_planet_;  // 7×17 = 119 TextCtrls für Planeten
    std::vector<wxTextCtrl*> text_ctrls_haus_;    // 7×12 = 84 TextCtrls für Häuser
    int current_typ_;  // 0=Radix, 1=Transit, 2=Synastrie
    bool show_houses_;  // false=Planeten, true=Häuser

    wxDECLARE_EVENT_TABLE();
};
